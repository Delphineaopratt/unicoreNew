
  # Build Interfaces

  This is a code bundle for Build Interfaces. The original project is available at https://www.figma.com/design/iE5ma7s5EWduniU0Kb36kF/Build-Interfaces.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  